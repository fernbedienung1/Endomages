\section{formating test for Formuals}
\fuck{testing Section - will drop out}
\begin{equation}
	\prod\int{\frac{\alpha * \epsilon}{ \beta - \frac{1}{3}}}
\end{equation}

inline assignments $x = 15$ and extra lines $$ y = 25 $$ are here.

\section{formating test for Code}
\fuck{testing Section - will drop out}
testing Python:
\begin{lstlisting}[language=Python]
#!/usr/bin/env	python3

import cv2
import numpy as np
import time

cap = cv2.VideoCapture(0)


while True :
	last_time=time.time()
	
	_, frame = cap.read()	# python _ --> not used
	hsv = cv2.cvtColor(frame, cv2.COLOR_BGR2HSV)

	# use unique color
	lower_grn = np.array([50, 40, 0]) 		#already HSV
	upper_grn = np.array([100, 255, 255])

	mask = cv2.inRange(hsv, lower_grn, upper_grn)	
	res = cv2.bitwise_and(frame, frame, mask = mask)
	
	
	cv2.imshow('frame', frame)
	cv2.imshow('mask', mask)
	cv2.imshow('res', res)


	print("FPS:\t%.2f" % (1/ (time.time() - last_time)), end='\r')
	k = cv2.waitKey(5) & 0xFF
	if k == 27:				# thats ESC
		break


cv2.destroyAllWindows()
cap.release()
\end{lstlisting}


And also a CPP test:

\begin{lstlisting}[language=c++]
	#include <iostream>
	
	int main(){
		std::cout << "hello World!\n" << std::flush;
		int i = 0;
		return i;	//very necessary comment 
	}
\end{lstlisting}
